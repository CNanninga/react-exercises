import ReactDOM from 'react-dom/client';
import App from './15_custom-hooks/index.js'

// ========================================

const root = ReactDOM.createRoot(
    document.getElementById('root')
);
root.render(<App />);
