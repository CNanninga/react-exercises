import ReactDOM from 'react-dom/client';
import App from './07_lists/index.js'

// ========================================

const root = ReactDOM.createRoot(
    document.getElementById('root')
);
root.render(<App />);
